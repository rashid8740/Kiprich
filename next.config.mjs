/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "source.unsplash.com",
      "via.placeholder.com",
      "cdn.usegalileo.ai",
      "unsplash.com",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
